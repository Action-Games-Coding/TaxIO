<UserControl x:Class="TaxIO.Frontend.UserControls.IncomeExcomeVisualizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaxIO.Frontend.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="✉"
                   FontSize="16"
                   VerticalAlignment="Center"
                   Margin="5,0,5,0"/>

        <TextBox Grid.Column="1"
                 Text="Název produktu výdaje nebo příjmu"
                 VerticalAlignment="Center"
                 Margin="0,5,5,5"
                 BorderThickness="0"/>

        <TextBlock Grid.Column="2"
                   Text="↓"
                   FontSize="16"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Margin="5,0"/>
        <Rectangle Grid.Column="2" Width="1" Fill="LightGray" VerticalAlignment="Stretch" Margin="0,5"/>

        <TextBox Grid.Column="3"
                 Text="částka"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Right"
                 Margin="5,5,5,5"
                 BorderThickness="0"
                 Width="80"/>
    </Grid>
</UserControl>
